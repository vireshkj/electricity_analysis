version: '3.8'

services:
  web:
    build: .
    ports:
      - "5001:5001"
    volumes:
      - .:/app
      - /app/__pycache__
    environment:
      - FLASK_ENV=development
    restart: unless-stopped
